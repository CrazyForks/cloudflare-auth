{
  "name": "cf-auth-example-worker-basic",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run --passWithNoTests",
    "dev": "wrangler dev"
  },
  "dependencies": {
    "@cf-auth/worker": "workspace:*"
  },
  "devDependencies": {
    "typescript": "6.0.3",
    "vitest": "4.1.6",
    "wrangler": "4.90.1"
  }
}
